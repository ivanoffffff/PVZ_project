spring.datasource.url=jdbc:mysql://localhost:3306/pvz
spring.datasource.username=epf
spring.datasource.password=ivan2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Initialisation de la base de données (facultatif)
spring.datasource.initialization-mode=always
spring.sql.init.mode=always

# Configuration du pool de connexions (facultatif)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# Afficher les requêtes SQL exécutées (utile pour le debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true